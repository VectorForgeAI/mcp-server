{
  "name": "@vectorforge/mcp-servers",
  "version": "0.1.3",
  "description": "Model Context Protocol servers for VectorForge APIs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "dev": "tsc --watch",
    "test:mcp": "npm run build && vitest run",
    "test": "npm run test:mcp"
  },
  "keywords": [
    "vectorforge",
    "mcp",
    "model-context-protocol",
    "ai",
    "divt"
  ],
  "author": "VectorForge",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.22.0",
    "@vectorforge-ai/sdk": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.0",
    "vitest": "^1.6.1"
  }
}
